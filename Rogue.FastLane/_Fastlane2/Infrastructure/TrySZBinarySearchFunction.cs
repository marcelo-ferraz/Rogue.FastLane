using System;

namespace Rogue.FastLane._Fastlane2.Infrastructure
{
    internal delegate bool TrySZBinarySearchFunction(Array sourceArray, int sourceIndex, int count, Object value, out int retVal);
}